node_modules/
.DS_Store
package-lock.json

# Private SEO working files + GSC data — never deploy publicly
_seo/

# Agent scratch — task briefs, reports, progress ledger
.superpowers/

# Design specs & implementation plans — deploy from repo root, so tracking
# these would publish keyword data and business constraints at /docs/
docs/

# AI mockup PNG masters (~2MB each) — only the WebP versions deploy
assets/images/mockups/*.png

# Audit & CRO working docs — deploy from repo root, so tracking these
# would publish GSC data and competitive analysis at /AUDIT-*.md
AUDIT-*.md
CRO-PLAN-*.md

# Image-generation working files, competitor price research and PNG masters —
# deploy is from repo root, so tracking these would publish them. Only the
# processed images in assets/images/products/<slug>/ deploy.
output/
outputs/
assets/images/products/*.png

__pycache__/

# Supplier quotes, pricing sources, minimums and internal notes for the product
# records — deploy is from repo root, so tracking these would publish them.
# Back this folder up yourself: it is NOT in git.
data/private/
